{
  "name": "@github1/smerge",
  "private": false,
  "version": "0.0.0",
  "description": "A javascript object merge module with support for deep merge or replace at particular paths within the target object",
  "main": "src/smerge.js",
  "scripts": {
    "build": "build-tools-build-lib",
    "test": "NO_SERVER=true build-tools test --coverage",
    "test:watch": "NO_SERVER=true build-tools test --watch --coverage",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@github1/build-tools": "0.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
